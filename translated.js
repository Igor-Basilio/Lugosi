
/* lug -> js os tipos podem ser discartados. */

function calculate( num1, num2 ) 
{
    let result_and;
    result_and = num1 + num2;
    return result_and;
}

// let float teste, res, fn;
let teste, res, fn;
// let int mof;
let mof;
// let bool valor;
let valor;
// let string hello;
let hello;

teste = 9.0E20;
console.log(teste);

hello = "Hello, World!\n" + "And Lugosi too!";
console.log(hello);

valor = true;
valor = ( 20 < 30 ) + 5;

// Em js todo o arquivo é a função main 
return 0;

function fib(n) 
{
    let n1, result;
    n1 = 0;

    while ( (n1 * 2) < n ) 
    {
        result = n1 + n2;
        n1 = ( n1 + 1 ) + ( 3 + 4 );
    }

    return result;
}

